name: Auto Merge Dependabot

on:
  workflow_run:
    workflows: [Build]
    types: [completed]

jobs:
  automerge:
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: write

    steps:
      - uses: fastify/github-action-merge-dependabot@v3
